import React, { useState } from 'react';
import { motion } from 'framer-motion';
import { 
  Search, 
  Eye,
  Download,
  FileText,
  File,
  FileImage,
  FileSpreadsheet,
  X
} from 'lucide-react';
import Button from '../components/ui/Button';
import '../styles/materials.css';

interface Material {
  id: number;
  name: string;
  category: string;
  description: string;
  file_type: string;
  file_size: string;
  download_url: string;
  tags: string[];
  upload_date: string;
  downloads: number;
}

const Materials: React.FC = () => {
  const [selectedMaterial, setSelectedMaterial] = useState<Material | null>(null);
  const [isPdfLoading, setIsPdfLoading] = useState(true);


  // Материалы для скачивания (документы, каталоги, сертификаты)
  const [materials, setMaterials] = useState<Material[]>([
    // Каталоги продукции
    {
      id: 1,
      name: 'Декоративная нержавеющая сталь',
      description: 'Полный каталог декоративной нержавеющей стали',
      category: 'catalogs',
      file_type: 'pdf',
      file_size: '6.6 MB',
      download_url: 'http://localhost:8000/Catalog/ИноксМеталАрт - Декоративная Нержавейка.pdf',
      tags: ['catalogs', 'PDF'],
      upload_date: '2023-11-25',
      downloads: 234
    },
    {
      id: 2,
      name: 'PressPlates™ каталог',
      description: 'Каталог пресс-форм и 3D узоров',
      category: 'catalogs',
      file_type: 'pdf',
      file_size: '3.0 MB',
      download_url: 'http://localhost:8000/Catalog/PressPlates™.pdf',
      tags: ['catalogs', 'PDF'],
      upload_date: '2024-01-02',
      downloads: 89
    },
    {
      id: 3,
      name: 'Стеганые листы',
      description: 'Каталог стеганых листов с 3D дизайнами',
      category: 'catalogs',
      file_type: 'pdf',
      file_size: '1.8 MB',
      download_url: 'http://localhost:8000/Catalog/Quilted.pdf',
      tags: ['catalogs', 'PDF'],
      upload_date: '2024-01-04',
      downloads: 67
    },
    {
      id: 4,
      name: 'Знаковая продукция',
      description: 'Каталог знаковой продукции (5 страниц)',
      category: 'catalogs',
      file_type: 'pdf',
      file_size: '1.2 MB',
      download_url: 'http://localhost:8000/Catalog/Sign products (5pages).pdf',
      tags: ['catalogs', 'PDF'],
      upload_date: '2024-01-02',
      downloads: 45
    },
    {
      id: 5,
      name: 'Индивидуальные поручни для лифтов',
      description: 'Каталог индивидуальных поручней из нержавеющей стали для лифтов',
      category: 'catalogs',
      file_type: 'pdf',
      file_size: '777 KB',
      download_url: 'http://localhost:8000/Catalog/1 Custom made Handrails for Elevators.pdf',
      tags: ['catalogs', 'PDF'],
      upload_date: '2024-01-12',
      downloads: 35
    },
    {
      id: 6,
      name: 'Декоративная нержавеющая сталь',
      description: 'Каталог декоративных решений из нержавеющей стали',
      category: 'catalogs',
      file_type: 'pdf',
      file_size: '2.8 MB',
      download_url: 'http://localhost:8000/Catalog/1 Decorative stainless steel.pdf',
      tags: ['catalogs', 'PDF'],
      upload_date: '2024-01-08',
      downloads: 28
    },
    {
      id: 7,
      name: 'Отделка лифтов нержавеющей сталью',
      description: 'Каталог отделки лифтов декоративной нержавеющей сталью',
      category: 'catalogs',
      file_type: 'pdf',
      file_size: '2.2 MB',
      download_url: 'http://localhost:8000/Catalog/1 Elevator decoration, stainless steel.pdf',
      tags: ['catalogs', 'PDF'],
      upload_date: '2024-01-06',
      downloads: 31
    },
    {
      id: 8,
      name: 'Наши производственные возможности',
      description: 'Каталог производственных возможностей компании',
      category: 'catalogs',
      file_type: 'pdf',
      file_size: '1.3 MB',
      download_url: 'http://localhost:8000/Catalog/Our Fabrication.pdf',
      tags: ['catalogs', 'PDF'],
      upload_date: '2023-11-24',
      downloads: 41
    },
    {
      id: 9,
      name: 'Знаковая продукция (5 страниц)',
      description: 'Каталог продукции для вывесок и указателей',
      category: 'catalogs',
      file_type: 'pdf',
      file_size: '1.2 MB',
      download_url: 'http://localhost:8000/Catalog/Sign products (5pages).pdf',
      tags: ['catalogs', 'PDF'],
      upload_date: '2023-11-20',
      downloads: 34
    },
    {
      id: 10,
      name: 'ИноксМеталАрт - Декоративная нержавеющая сталь',
      description: 'Полный каталог компании ИноксМеталАрт - декоративная нержавеющая сталь',
      category: 'catalogs',
      file_type: 'pdf',
      file_size: '6.6 MB',
      download_url: 'http://localhost:8000/Catalog/ИноксМеталАрт - Декоративная Нержавейка.pdf',
      tags: ['catalogs', 'PDF'],
      upload_date: '2023-11-15',
      downloads: 67
    },
    // Сертификаты качества
    {
      id: 11,
      name: 'Material Safety RoHS - NAS coating',
      description: 'Сертификат безопасности материалов RoHS для NAS покрытия',
      category: 'certificates',
      file_type: 'pdf',
      file_size: '238 KB',
      download_url: 'http://localhost:8000/Catalog/Material Safety RoHS - NAS coating.pdf',
      tags: ['certificates', 'PDF'],
      upload_date: '2023-12-10',
      downloads: 58
    },
    {
      id: 12,
      name: 'Material Safety RoHS - Ti(PVD) coating',
      description: 'Сертификат безопасности материалов RoHS для Ti(PVD) покрытия',
      category: 'certificates',
      file_type: 'pdf',
      file_size: '381 KB',
      download_url: 'http://localhost:8000/Catalog/Material Safety RoHS - Ti(PVD) coating (Hwa Lin).pdf',
      tags: ['certificates', 'PDF'],
      upload_date: '2023-12-20',
      downloads: 15
    },
    {
      id: 13,
      name: 'NAS™ Anti-fingerprint coated SS sheets',
      description: 'Сертификат NAS™ антипечатного покрытия для нержавеющих листов',
      category: 'certificates',
      file_type: 'pdf',
      file_size: '654 KB',
      download_url: 'http://localhost:8000/Catalog/NAS™ Anti-fingerprint coated SS sheets.pdf',
      tags: ['certificates', 'PDF'],
      upload_date: '2023-12-15',
      downloads: 32
    },
    {
      id: 14,
      name: 'N8 Mirror Surface Scan Test',
      description: 'Результаты сканирования поверхности N8 Mirror',
      category: 'specifications',
      file_type: 'jpg',
      file_size: '110 KB',
      download_url: 'http://localhost:8000/Catalog/N8 Mirror Surface Scan Test.jpg',
      tags: ['specifications', 'JPG'],
      upload_date: '2023-12-08',
      downloads: 24
    },
    {
      id: 15,
      name: 'sNAS coating Anti Microbial test',
      description: 'Результаты тестирования антимикробных свойств sNAS покрытия',
      category: 'specifications',
      file_type: 'jpg',
      file_size: '181 KB',
      download_url: 'http://localhost:8000/Catalog/sNAS coating Anti Microbial test.jpg',
      tags: ['specifications', 'JPG'],
      upload_date: '2023-12-06',
      downloads: 19
    },
    {
      id: 16,
      name: 'Material Safety RoHS - AISI 316 grade.pdf',
      description: 'Дополнительная документация по безопасности материалов RoHS',
      category: 'certificates',
      file_type: 'pdf',
      file_size: '0.7 MB',
      download_url: 'http://localhost:8000/Catalog/Material Safety RoHS - AISI 316 grade.pdf',
      tags: ['certificates', 'PDF'],
      upload_date: '2023-11-10',
      downloads: 12
    },
    {
      id: 17,
      name: 'Material Safety RoHS - AISI 430 grade (Hwa Lin).pdf',
      description: 'Расширенная документация по соответствию RoHS',
      category: 'certificates',
      file_type: 'pdf',
      file_size: '0.8 MB',
      download_url: 'http://localhost:8000/Catalog/Material Safety RoHS - AISI 430 grade (Hwa Lin).pdf',
      tags: ['certificates', 'PDF'],
      upload_date: '2023-11-08',
      downloads: 14
    },
    {
      id: 18,
      name: 'Material Safety RoHS - Ti(PVD) coating (Hwa Lin).pdf',
      description: 'Полная документация по экологической безопасности',
      category: 'certificates',
      file_type: 'pdf',
      file_size: '0.9 MB',
      download_url: 'http://localhost:8000/Catalog/Material Safety RoHS - Ti(PVD) coating (Hwa Lin).pdf',
      tags: ['certificates', 'PDF'],
      upload_date: '2023-11-06',
      downloads: 13
    },
    // Технические спецификации
    {
      id: 19,
      name: 'Mirror Stainless Steel.pdf',
      description: 'Техническая спецификация зеркальной нержавеющей стали',
      category: 'specifications',
      file_type: 'pdf',
      file_size: '1.5 MB',
      download_url: 'http://localhost:8000/Catalog/Mirror Stainless Steel.pdf',
      tags: ['specifications', 'PDF'],
      upload_date: '2023-12-01',
      downloads: 23
    },
    {
      id: 20,
      name: 'N8 Mirror Surface Stainless Steel (8pags).pdf',
      description: 'Зеркальная поверхность N8 - техническое описание',
      category: 'specifications',
      file_type: 'pdf',
      file_size: '2.3 MB',
      download_url: 'http://localhost:8000/Catalog/N8 Mirror Surface Stainless Steel (8pags).pdf',
      tags: ['specifications', 'PDF'],
      upload_date: '2023-11-28',
      downloads: 27
    },
    {
      id: 21,
      name: 'NAS™ Anti-fingerprint Stainless Steel.pdf',
      description: 'Антипечатная нержавеющая сталь NAS™',
      category: 'specifications',
      file_type: 'pdf',
      file_size: '1.8 MB',
      download_url: 'http://localhost:8000/Catalog/NAS™ Anti-fingerprint Stainless Steel.pdf',
      tags: ['specifications', 'PDF'],
      upload_date: '2023-11-26',
      downloads: 33
    },
    {
      id: 22,
      name: 'sNAS coating Anti-fingerprint Stainless Steel.pdf',
      description: 'Покрытие sNAS против отпечатков пальцев',
      category: 'specifications',
      file_type: 'pdf',
      file_size: '1.4 MB',
      download_url: 'http://localhost:8000/Catalog/sNAS coating Anti-fingerprint Stainless Steel.pdf',
      tags: ['specifications', 'PDF'],
      upload_date: '2023-11-18',
      downloads: 21
    },
    {
      id: 23,
      name: 'PressPlates™.pdf',
      description: 'Гибочные станки и технологии обработки',
      category: 'specifications',
      file_type: 'pdf',
      file_size: '2.0 MB',
      download_url: 'http://localhost:8000/Catalog/PressPlates™.pdf',
      tags: ['specifications', 'PDF'],
      upload_date: '2023-11-22',
      downloads: 26
    },
    {
      id: 24,
      name: 'Floor Plates (FP).pdf',
      description: 'Напольные пластины из нержавеющей стали',
      category: 'specifications',
      file_type: 'pdf',
      file_size: '1.9 MB',
      download_url: 'http://localhost:8000/Catalog/Floor Plates (FP).pdf',
      tags: ['specifications', 'PDF'],
      upload_date: '2023-12-16',
      downloads: 22
    },
    {
      id: 25,
      name: 'Hollow Spheres.pdf',
      description: 'Полые сферы из нержавеющей стали',
      category: 'specifications',
      file_type: 'pdf',
      file_size: '1.5 MB',
      download_url: 'http://localhost:8000/Catalog/Hollow Spheres.pdf',
      tags: ['specifications', 'PDF'],
      upload_date: '2023-12-14',
      downloads: 18
    },
    {
      id: 26,
      name: '1000hrs. Accelerated Salt Spray Test Certificate (PVD coating).pdf',
      description: 'Применение покрытий с 1000-часовой устойчивостью',
      category: 'specifications',
      file_type: 'pdf',
      file_size: '1.3 MB',
      download_url: 'http://localhost:8000/Catalog/1000hrs. Accelerated Salt Spray Test Certificate (PVD coating).pdf',
      tags: ['specifications', 'PDF'],
      upload_date: '2023-11-12',
      downloads: 16
    },
    // Дизайны и каталоги
    {
      id: 27,
      name: 'Art Etching Designs (preview).pdf',
      description: 'Каталог художественных дизайнов для травления нержавеющей стали',
      category: 'designs',
      file_type: 'pdf',
      file_size: '0.9 MB',
      download_url: 'http://localhost:8000/Catalog/Art Etching Designs (preview).pdf',
      tags: ['designs', 'PDF'],
      upload_date: '2024-01-15',
      downloads: 25
    },
    {
      id: 28,
      name: 'ArtBrush.pdf',
      description: 'Коллекция текстур и эффектов для художественной обработки поверхности',
      category: 'designs',
      file_type: 'pdf',
      file_size: '1.9 MB',
      download_url: 'http://localhost:8000/Catalog/ArtBrush.pdf',
      tags: ['designs', 'PDF'],
      upload_date: '2024-01-10',
      downloads: 18
    },
    {
      id: 29,
      name: 'DesignTrims™.pdf',
      description: 'Декоративные накладки и элементы отделки из нержавеющей стали',
      category: 'designs',
      file_type: 'pdf',
      file_size: '3.4 MB',
      download_url: 'http://localhost:8000/Catalog/DesignTrims™.pdf',
      tags: ['designs', 'PDF'],
      upload_date: '2024-01-05',
      downloads: 42
    }
  ]);
  const [loading, setLoading] = useState(false);



  // Получение материалов с сервера (временно отключено)
  React.useEffect(() => {
    // Временно используем моковые данные вместо API
        setLoading(false);
    
    // const fetchMaterials = async () => {
    //   setLoading(true);
    //   try {
    //     const response = await fetch('http://localhost:8000/api/v1/materials/');
    //     if (response.ok) {
    //       const data = await response.json();
    //       setMaterials(data.materials || []);
    //     } else {
    //       setError('Ошибка загрузки материалов');
    //     }
    //   } catch (err) {
    //     setError('Ошибка соединения с сервером');
    //     console.error('Error fetching materials:', err);
    //   } finally {
    //     setLoading(false);
    //   }
    // };

    // fetchMaterials();
  }, []);

  // Сброс состояния загрузки PDF при открытии модального окна
  React.useEffect(() => {
    if (selectedMaterial) {
      setIsPdfLoading(true);
    }
  }, [selectedMaterial]);

  // Функция скачивания материала
  const handleDownload = async (material: Material) => {
    try {
      // Показываем индикатор загрузки
      const downloadButton = document.querySelector(`[data-material-id="${material.id}"]`);
      if (downloadButton) {
        downloadButton.innerHTML = '<div className="animate-spin rounded-full h-4 w-4 border-b-2 border-white mx-auto"></div>';
        downloadButton.setAttribute('disabled', 'true');
      }

      // Увеличиваем счетчик скачиваний
      await fetch(`http://localhost:8000/api/v1/materials/${material.id}/download`, {
        method: 'POST',
      });

      // Скачиваем файл
      const response = await fetch(material.download_url);
      if (response.ok) {
        const blob = await response.blob();
        const url = window.URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url;
        a.download = material.name;
        document.body.appendChild(a);
        a.click();
        window.URL.revokeObjectURL(url);
        document.body.removeChild(a);
        
        // Обновляем локальное состояние
        setMaterials(prev => prev.map(m => 
          m.id === material.id ? { ...m, downloads: m.downloads + 1 } : m
        ));

        // Показываем уведомление об успехе
        if (downloadButton) {
          downloadButton.innerHTML = '<CheckCircle className="w-4 h-4 mx-auto text-green-500" />';
          setTimeout(() => {
            if (downloadButton) {
              downloadButton.innerHTML = '<Download className="w-4 h-4 mr-2" />Скачать';
              downloadButton.removeAttribute('disabled');
            }
          }, 2000);
        }
      } else {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
    } catch (error) {
      console.error('Ошибка скачивания:', error);
      alert(`Ошибка при скачивании файла: ${error instanceof Error ? error.message : 'Неизвестная ошибка'}`);
      
      // Восстанавливаем кнопку
      const downloadButton = document.querySelector(`[data-material-id="${material.id}"]`);
      if (downloadButton) {
        downloadButton.innerHTML = '<Download className="w-4 h-4 mr-2" />Скачать';
        downloadButton.removeAttribute('disabled');
      }
    }
  };

  const filteredMaterials = materials; // Показываем все материалы без фильтрации

  // Группировка материалов по категориям
  const groupedMaterials = filteredMaterials.reduce((groups, material) => {
    const category = material.category;
    if (!groups[category]) {
      groups[category] = [];
    }
    groups[category].push(material);
    return groups;
  }, {} as Record<string, Material[]>);

  // Названия категорий на русском языке
  const categoryNames: Record<string, string> = {
    'catalogs': 'Каталоги продукции',
    'certificates': 'Сертификаты качества',
    'specifications': 'Технические спецификации',
    'designs': 'Дизайны и каталоги'
  };


  const getFileIcon = (fileType: string) => {
    switch (fileType.toLowerCase()) {
      case 'pdf':
        return FileText;
      case 'image':
      case 'jpg':
      case 'jpeg':
      case 'png':
        return FileImage;
      case 'excel':
      case 'xlsx':
      case 'xls':
        return FileSpreadsheet;
      case 'doc':
      case 'docx':
        return FileText;
      default:
        return File;
    }
  };



  // Детальная страница заменена модальным окном ниже

  return (
    <div className="min-h-screen bg-white pt-32">
      {/* Simple Header */}
      <section className="py-8 bg-white">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h1 className="text-3xl font-bold text-gray-900 text-center mb-2">
            Материалы
          </h1>
          <p className="text-gray-600 text-center">
            Полезные материалы и документация для скачивания
          </p>
        </div>
      </section>


      {/* Materials Grid */}
      <section className="py-20 bg-white">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          {loading ? (
            <div className="text-center py-20">
              <div className="w-24 h-24 bg-gray-100 rounded-full mx-auto mb-6 flex items-center justify-center">
                <div className="w-8 h-8 border-4 border-blue-600 border-t-transparent rounded-full animate-spin"></div>
              </div>
              <h3 className="text-xl font-semibold text-gray-900 mb-2">
                Загрузка материалов...
              </h3>
              <p className="text-gray-600">
                Пожалуйста, подождите
              </p>
            </div>
          ) : materials.length === 0 ? (
            <div className="text-center py-20">
              <div className="w-24 h-24 bg-gray-100 rounded-full mx-auto mb-6 flex items-center justify-center">
                <FileText className="w-12 h-12 text-gray-400" />
              </div>
              <h3 className="text-xl font-semibold text-gray-900 mb-2">
                Материалы не найдены
              </h3>
              <p className="text-gray-600">
                В данный момент нет доступных материалов для скачивания
              </p>
            </div>
          ) : (
            <div className="space-y-12">
              {Object.entries(groupedMaterials).map(([category, materials]) => (
                <div key={category} className="space-y-6">
                  <div className="border-b border-gray-200 pb-4">
                    <h2 className="text-2xl font-bold text-gray-900">
                      {categoryNames[category] || category}
                    </h2>
                    <p className="text-gray-600 mt-2">
                      {materials.length} {materials.length === 1 ? 'материал' : 'материалов'}
                    </p>
                  </div>
                  <div className="space-y-2">
                    {materials.map((material, index) => (
                      <motion.div
                        key={material.id}
                        initial={{ opacity: 0, y: 20 }}
                        animate={{ opacity: 1, y: 0 }}
                        transition={{ duration: 0.4, delay: index * 0.05 }}
                        className="bg-white rounded border border-gray-200 hover:border-blue-300 hover:bg-blue-50/30 transition-all duration-200"
                      >
                        <div className="px-3 py-2">
                          <div className="flex items-center justify-between">
                            {/* Material Info */}
                            <div className="flex items-center space-x-2 flex-1">
                              {/* File Icon */}
                              <div className="w-6 h-6 bg-blue-100 rounded flex items-center justify-center flex-shrink-0">
                                {React.createElement(getFileIcon(material.file_type), { className: "w-3 h-3 text-blue-600" })}
                             </div>

                              {/* Content */}
                              <div className="flex-1 min-w-0">
                                <h4 className="text-sm font-semibold text-gray-900 truncate">{material.name}</h4>
                                <p className="text-xs text-gray-600 mb-1 line-clamp-1">{material.description}</p>
                                <div className="flex items-center space-x-3 text-xs text-gray-500">
                                  <span className="bg-gray-100 px-2 py-0.5 rounded text-xs">
                                    {material.file_type?.toUpperCase() || 'PDF'}
                                  </span>
                                  <span>{material.file_size || 'Н/Д'}</span>
                                  <span>•</span>
                                  <span>{material.downloads} скачиваний</span>
                                </div>
                              </div>
                            </div>
                            
                            {/* Action Buttons */}
                            <div className="flex items-center space-x-1 flex-shrink-0">
                            <Button 
                              variant="outline" 
                              size="sm" 
                                onClick={() => setSelectedMaterial(material)}
                                className="px-2 py-1 text-xs h-6"
                              >
                                <Eye className="w-3 h-3" />
                            </Button>
                            <Button 
                                variant="primary"
                              size="sm" 
                                onClick={() => handleDownload(material)}
                                className="px-2 py-1 text-xs h-6"
                              >
                                <Download className="w-3 h-3" />
                            </Button>
                            </div>
                          </div>
                        </div>
                      </motion.div>
                    ))}
                  </div>
                </div>
              ))}
            </div>
          )}

          {/* No Results */}
          {filteredMaterials.length === 0 && materials.length > 0 && (
            <motion.div
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              className="text-center py-20"
            >
              <div className="w-24 h-24 bg-gray-100 rounded-full mx-auto mb-6 flex items-center justify-center">
                <Search className="w-12 h-12 text-gray-400" />
              </div>
              <h3 className="text-xl font-semibold text-gray-900 mb-2">
                Материалы не найдены
              </h3>
              <p className="text-gray-600">
                Попробуйте выбрать другую категорию или свяжитесь с нами
              </p>
            </motion.div>
          )}
        </div>
      </section>

      {/* CTA Section */}
      <section className="py-20 bg-gradient-to-br from-blue-900 to-blue-800">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: true }}
          >
            <h2 className="text-4xl font-bold text-white mb-6">
              Нужны дополнительные материалы?
            </h2>
            <p className="text-xl text-blue-100 mb-8 max-w-3xl mx-auto">
              Свяжитесь с нами для получения дополнительной документации, 
              технических спецификаций или индивидуальных консультаций.
            </p>
            <div className="flex flex-col sm:flex-row gap-4 justify-center">
              <Button variant="secondary" size="lg">
                Связаться с нами
              </Button>
              <Button variant="outline" size="lg" className="border-white text-white hover:bg-white hover:text-blue-900">
                Запросить материалы
              </Button>
            </div>
          </motion.div>
        </div>
      </section>

      {/* Modal: Material PDF Viewer */}
      {selectedMaterial && (
        <div
          className="fixed inset-0 bg-black/50 backdrop-blur-sm z-[1000] flex items-center justify-center p-6"
          onClick={(e) => {
            if (e.currentTarget === e.target) setSelectedMaterial(null);
          }}
        >
          <motion.div
            initial={{ opacity: 0, scale: 0.97 }}
            animate={{ opacity: 1, scale: 1 }}
            exit={{ opacity: 0, scale: 0.97 }}
            transition={{ duration: 0.2 }}
            className="bg-white rounded-2xl shadow-2xl w-full max-w-[95vw] h-[90vh] overflow-hidden flex flex-col"
          >
            {/* Header */}
            <div className="flex items-center justify-between p-4 border-b border-gray-200 flex-shrink-0">
              <div className="flex items-center gap-3">
                <div className="w-10 h-10 bg-gradient-to-br from-blue-600 to-blue-800 rounded-lg flex items-center justify-center">
                  {React.createElement(getFileIcon(selectedMaterial.file_type), { className: 'w-5 h-5 text-white' })}
                </div>
                <div>
                  <h3 className="text-lg font-bold text-gray-900">{selectedMaterial.name}</h3>
                  <p className="text-sm text-gray-600">{selectedMaterial.description}</p>
                </div>
              </div>
              <button 
                className="p-2 hover:bg-gray-100 rounded-lg transition-colors" 
                onClick={() => setSelectedMaterial(null)}
              >
                <X className="w-5 h-5" />
              </button>
            </div>

                        {/* PDF Viewer */}
            <div className="flex-1 overflow-hidden relative" style={{ height: 'calc(90vh - 140px)' }}>
              {isPdfLoading && (
                <div className="absolute inset-0 bg-white flex items-center justify-center z-10">
                  <div className="text-center">
                    <div className="w-8 h-8 border-4 border-blue-600 border-t-transparent rounded-full animate-spin mx-auto mb-2"></div>
                    <p className="text-gray-600">Загрузка документа...</p>
                </div>
                </div>
              )}
              <iframe
                src={`${selectedMaterial.download_url}#toolbar=0&navpanes=0&scrollbar=0`}
                title={selectedMaterial.name}
                className="w-full h-full border-0"
                loading="lazy"
                onLoad={() => setIsPdfLoading(false)}
              />
                </div>

            {/* Footer with Actions */}
            <div className="flex items-center justify-between p-4 border-t border-gray-200 bg-gray-50 flex-shrink-0">
              <div className="flex items-center space-x-4 text-sm text-gray-600">
                <span className="bg-gray-200 px-2 py-1 rounded text-xs">
                  {selectedMaterial.file_type?.toUpperCase() || 'PDF'}
                  </span>
                <span>{selectedMaterial.file_size}</span>
                <span>• {selectedMaterial.downloads} скачиваний</span>
              </div>
              
              <div className="flex items-center space-x-2">
                <Button
                  variant="primary"
                  onClick={() => handleDownload(selectedMaterial)}
                  className="text-sm"
                >
                  <Download className="w-4 h-4 mr-1" />
                  Скачать PDF
                </Button>
              </div>
            </div>
          </motion.div>
        </div>
      )}
    </div>
  );
};

export default Materials;
